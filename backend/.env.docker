# Docker Environment Configuration
# Copy this file to .env when running with Docker

# Database Configuration
DB_USER=fintrax
DB_PASSWORD=fintrax123
DB_NAME=fintrax_db
DB_HOST=postgres
DB_PORT=5432

# Application Configuration
APP_PORT=:8080
GIN_MODE=debug

# Security
JWT_SECRET=change-this-secret-in-production-to-something-secure

# Email Configuration (Optional - for OTP functionality)
EMAIL=
EMAIL_PASSWORD=
